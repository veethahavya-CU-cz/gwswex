# Benchmark scratch and runtime outputs (regenerated by run_*.py + analyse.py).
# Scripts and README stay versioned; everything below is reproducible from them.
results/*.json
results/*.csv
results/*.pdf
results/*.png
tmp/
